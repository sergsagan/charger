header.header
    .header_logo
        a.logo(href="index.html")
            img.logo_icon(src="../img/svg/icon.svg")
            span.logo_text Charger
                span CUSTOMS

    .header_nav
        - var nav = {'about us': 'index.html', 'Services': 'index2.html', 'projects': 'index3.html'}

        ul
            each linkPath, linkName in nav
                li: a(href= linkPath) #{linkName}
    .header_menu
        img.menu_icon(src="../img/svg/menu.svg")
        span.menu_text Menu

